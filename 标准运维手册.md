# 标准运维手册

## 1. 项目基础信息
- **项目名称**: Deep Dark Theme
- **项目类型**: VS Code Extension (Color Theme)
- **代码仓库**: https://github.com/geniu/deep-dark-theme
- **当前版本**: v0.4.17
- **技术栈**: JSON, VS Code Extension API

## 2. 环境配置
### 本地开发环境
- **系统**: Windows / macOS / Linux
- **依赖**: 
  - Node.js (推荐 v16+)
  - VS Code
  - `vsce` (Visual Studio Code Extensions CLI)
    ```bash
    npm install -g @vscode/vsce
    ```

### 调试方法
1. 在 VS Code 中打开项目根目录。
2. 按 `F5` 启动调试窗口（Extension Development Host）。
3. 在新窗口中按 `Ctrl+K Ctrl+T` 切换到 "Deep Dark" 主题进行预览。

## 3. 部署与发布流程
### 发布到 VS Code Marketplace
> **注意**: 发布前请确保已更新 `package.json` 中的版本号。

1. **打包与验证**:
   ```bash
   vsce package
   ```
   此命令会生成 `.vsix` 文件，可用于本地测试安装。

2. **发布**:
   ```bash
   vsce publish
   ```
   *需要 Personal Access Token (PAT)*。

3. **Git 同步**:
   - 提交代码并推送到远程仓库。
   - 打上版本 Tag (可选)。

## 4. 账号与凭证
- **Publisher**: `geniu`
- **Marketplace Token**: (请在此处安全存储或指向 1Password/Bitwarden)

## 5. 常用命令
- `git status` - 查看状态
- `git pull` - 拉取更新
- `vsce package` - 打包扩展

## 6. 完整目录结构说明
- `.vscode/` - VS Code 本地开发配置
- `themes/` - 包含主题的 JSON 配置文件
  - `deep-dark-color-theme.json` - 具体深色主题颜色定义
- `package.json` - 扩展的清单文件，包含配置和依赖
- `README.md` - 扩展说明文档
- `标准运维手册.md` - 项目运维手册
- `AI_CONVERSATION_LOG.md` - AI 操作记录文档

## 7. 环境变量清单
- **环境变量**: 本项目无任何环境变量要求或配置。
- **数据库配置**: 无（本项目不使用数据库）。
- **第三方API**: 无。
- **云端发布服务**: 通过 `vsce publish` 直接发布至 VS Code Marketplace，不涉及传统云服务器。

## 8. 版本历史
- **v0.4.17**: 更新了主题，去除所有边框（统一为 #202124）。
